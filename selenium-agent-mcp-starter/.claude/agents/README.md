# Claude Agents

This folder is reserved for Claude-compatible agent definitions.

In this starter, the canonical AI implementation lives under:

```text
ai/agents/
```

If you wire this repo into a Claude workflow that expects `.claude/agents`, mirror or symlink the files from `ai/agents/` into this directory.
