# API Tests

This Selenium starter is primarily focused on browser-driven UI coverage.

The `tests/api/` folder is reserved for lightweight service or endpoint checks that may support UI journeys later.
If you do not need API coverage in this repo, you can keep using the browser suites only.
